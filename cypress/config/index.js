// Processed and loaded automatically before your test files.
// This is a great place to put global configuration that modifies Cypress.

// Example:
// Cypress.Commands.add('login', (email, password) => { ... });
// Cypress.Commands.overwrite('visit', (fn, url, options) => { ... });
